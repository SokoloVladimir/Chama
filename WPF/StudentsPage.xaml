<Page x:Class="WPF.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Отображение студентов">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" PreviewMouseDoubleClick="DataGrid_PreviewMouseDoubleClick" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Surname}" Header="Фамилия"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Имя"/>
                <DataGridTextColumn Binding="{Binding Patronym}" Header="Отчетство"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel>
            <Button Content=">>" HorizontalAlignment="Right" Click="NavExportButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Button Content="Новая запись" Click="NewRecordButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
