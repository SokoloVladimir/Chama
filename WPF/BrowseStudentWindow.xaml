<Window x:Class="WPF.BrowseStudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="Выбрать студента" Height="112" Width="350"
        Icon="pack://application:,,,/Resources/circle.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Content="Группа" Grid.Column="0" Grid.Row="0"/>
        <ComboBox ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" Grid.Column="1" Grid.Row="0" SelectionChanged="GroupComboBox_SelectionChanged"/>

        <Label Content="Студент" Grid.Column="0" Grid.Row="1"/>
        <ComboBox ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" Grid.Column="1" Grid.Row="1"/>

        <Button Content="Выбрать" Height="Auto" Grid.ColumnSpan="2" Grid.Row="2" Click="BrowseButton_Click"/>
    </Grid>
</Window>
